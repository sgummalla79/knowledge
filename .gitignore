__pycache__/
*.pyc
.env
.venv/
venv/
.claude/
.pytest_cache/

# webui/ build output — generated by `npm run build` / deploy/Dockerfile's Node stage, not
# committed source. webui/package-lock.json IS committed (npm ci needs it).
webui/node_modules/
app/static/workspace/

# Local scratch content, not part of the app
Knowledge data library pages/
