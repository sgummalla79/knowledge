POSTGRES_DB=rag
POSTGRES_USER=rag
POSTGRES_PASSWORD=
SECRET_KEY=
PORT=13102
# MCP_BASE_URL=http://127.0.0.1:13102   # optional, default shown — the origin the three MCP tool
                                         # tiers (/mcp/search, /mcp/read, /mcp/write) are served from;
                                         # same origin as PORT above since they share this process
